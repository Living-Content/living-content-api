# ----------------------------------------------------------------
# Secret configurations for sensitive data
# ----------------------------------------------------------------
living_content:
  api_key: ${UUID4}

aws:
  account_id:
  access_key_id:
  secret_access_key:

mongo:
  db_name: living-content
  host: mongo
  port: 27017
  root_username: root
  root_password: ${PASSWORD}
  rw_username: living-content-rw
  rw_password: ${PASSWORD}

redis:
  host: redis
  port: 6379
  password: ${PASSWORD}

openai:
  api_key:

eqty:
  trusted_host_username:
  trusted_host_password:
